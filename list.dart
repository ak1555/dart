void main(){
  List<dynamic> ls=[1,2,3,4,5];


  
  // ls.add(8);
  // ls.addAll([5,6,7]);
  // ls.remove(5);
  
  //print(ls.length);
  // print(ls.first);
  // print(ls.isEmpty);
  // print(ls.isNotEmpty);
  // print(ls.last);
  // print(ls.reversed.toList());
  // ls.add('hi');
  // print(ls);

}